import VueRouter from "vue-router";
import VueRouter from "vue-router";
import Vue from "vue";

import BookDetail from "../pages/BookDetail";

Vue.use(VueRouter);

const router = new VueRouter({
  routes: [
    {
        path:"book"
    }
],
});
